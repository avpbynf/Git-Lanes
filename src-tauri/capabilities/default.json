{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the one window this app opens is allowed to do",
  "windows": ["main"],
  "permissions": ["core:default"]
}
